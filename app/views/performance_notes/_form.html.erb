<div>
  <%= form_for(@performance_note, remote: true) do |f| %>
    <h4><%= @performance.title.blank? ? "<i>No Title Specified</i>".html_safe : @performance.title %></h4>
    <%= f.hidden_field :performance_id, value: @performance.id %>
    <%= f.hidden_field :creator, value: User.current_username %>
    <table>
      <tbody>
      <tr>
        <th>Note Creator</th>
        <td><%= User.current_username %></td>
      </tr>
      <tr>
        <th>Note Text</th>
        <td>
          <div class="textarea_wrap">
            <%= f.text_area :text, class: 'tArea' %>
          </div>
        </td>
      </tr>
      </tbody>
    </table>
  <% end %>
</div>