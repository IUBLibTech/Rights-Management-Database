
# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
db/*.sqlite3
db/*.sqlite3-journal

# Ignore all logfiles and tempfiles.
log/*
!/log/.keep
tmp
config/database.yml
config/pod_db.yml

# files for rubymine
.idea/*

# files from Passenger
passenger.*

db/schema.rb

# settings contains the service credentials for push/pull pod/filmdb accounts, so don't include
config/settings/development.yml
config/settings/test.yml
config/settings/production.yml
config/secrets.yml

config/settings.local.yml
config/settings/*.local.yml
config/environments/*.local.yml

public/
.byebug_history
